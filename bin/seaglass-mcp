#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -P -- "$(dirname -- "$0")/.." && pwd)
PYTHON="$ROOT/.venv/bin/python"

if [ ! -x "$PYTHON" ]; then
    printf '%s\n' \
        "seaglass-mcp: $PYTHON is missing; run $ROOT/setup.sh first" >&2
    exit 1
fi

exec "$PYTHON" -m seaglass.mcp_server
